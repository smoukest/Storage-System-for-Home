<Window x:Class="ApartmentInventory.Views.AddRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить комнату" Height="280" Width="480"
        WindowStartupLocation="CenterOwner" 
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid VerticalAlignment="Center" Margin="25">
        <StackPanel VerticalAlignment="Top">
            <TextBlock Text="Название комнаты:" FontSize="14" Foreground="#333" FontWeight="Bold" Margin="0,0,0,8"/>
            <TextBox x:Name="RoomNameTextBox" Height="38" Padding="10" FontSize="13" Margin="0,0,0,15"
                     BorderThickness="1" BorderBrush="#CCC"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="OK" Width="85" Height="38" Click="OkButton_Click" 
                        Background="#4CAF50" Foreground="White" Cursor="Hand" Margin="0,0,10,0" FontSize="12"/>
                <Button Content="Отмена" Width="85" Height="38" IsCancel="True" 
                        Background="#808080" Foreground="White" Cursor="Hand" FontSize="12"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>